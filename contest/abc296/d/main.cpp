#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n, m;

    cin >> n >> m;

    if (n >= m)
    {
        cout << m << endl;
        return 0;
    }

        cout < < < < endl;
    return 0;
}