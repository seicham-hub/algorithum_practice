#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < n; ++i)

int main()
{

    rep(i, 4)
    {
        int is_left_top = true;
        rep(i, 4)
        {
            char c;
            cin >> c;
            if (c == '#')
            {
                if (is_left_top)
            }
        }
    }

    return 0;
}